var brandOptions = {
    valueNames: ['game-name', 'founded-year', 'jenre', 'developer', 'platforms']
};

var brandList = new List('car-brands', brandOptions);

var modelOptions = {
    valueNames: ['game-name', 'os', 'processor', 'ram',
                 'hdd', 'video-card']
};

var modelList = new List('car-models', modelOptions);